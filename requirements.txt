# --- Core HTTP + API automation (Sprints 4, 5) ---
requests==2.32.3

# --- Test framework + reporting (Sprints 5, 6) ---
pytest==8.3.3
pytest-html==4.1.1

# --- UI automation (Sprint 6) — Selenium Manager auto-resolves the driver ---
selenium==4.25.0

# --- Config / env handling ---
python-dotenv==1.0.1
